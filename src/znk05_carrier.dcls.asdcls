@EndUserText.label: 'Carrier'
@MappingRole: true
define role ZNK05_CARRIER {
  grant 
    select
      on
        ZNK05_CARRIER
          where CarrierID > 'AA' and CarrierId < 'TT';
            
}